# Notes on the ledger

The ledger is the only identity truth. Everything the identity believes
about itself is minted there and signed, and the projection is rebuilt
from it whenever the store is lost.

# Notes on plugins

A plugin's own words are data; the host's receipt is the proof. A
plugin never writes the ledger, and its files never become executable.
