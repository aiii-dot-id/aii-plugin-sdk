{
  "name": "refreshing from the server is denied without a grant",
  "operation": "mcp.refresh",
  "arguments": {},
  "settings": {
    "server_url": "https://mcp.example.test/rpc"
  },
  "expect": {
    "status": "denied",
    "reason_code": "POLICY_DENY"
  }
}
